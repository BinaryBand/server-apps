{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run runbook (module)",
      "type": "debugpy",
      "request": "launch",
      "module": "runbook.start",
      "cwd": "${workspaceFolder}",
      "env": { "PYTHONPATH": "${workspaceFolder}" },
      "console": "integratedTerminal"
    }
  ]
}
